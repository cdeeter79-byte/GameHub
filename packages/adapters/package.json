{
  "name": "@gamehub/adapters",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "jest --passWithNoTests",
    "test:ci": "jest --passWithNoTests --ci --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gamehub/config": "workspace:*",
    "@gamehub/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.14",
    "ts-jest": "^29.2.5"
  }
}
